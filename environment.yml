name: binary-waterfall
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10*
  - pip
  - tk
  - opencv
  - pillow
  - pip:
    - pyinstaller
    - pyinstaller-versionfile
    - pygame
    - numpy